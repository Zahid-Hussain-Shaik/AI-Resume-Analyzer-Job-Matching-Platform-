import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import morgan from 'morgan'; import swaggerUi from 'swagger-ui-express'; import { env } from './config/env.js'; import authRoutes from './routes/auth.js'; import resumeRoutes from './routes/resume.js'; import analysisRoutes from './routes/analysis.js'; import { auth } from './middleware/auth.js'; import { errorHandler } from './middleware/error.js'; import { swaggerDocument } from './config/swagger.js';
export const app = express(); app.use(helmet()); app.use(cors({ origin: env.CLIENT_ORIGIN })); app.use(express.json({ limit: '1mb' })); app.use(morgan('combined')); app.get('/', (_req, res) => res.json({ success: true, message: 'AI Resume Analyzer API', health: '/health', documentation: '/docs', apiBase: '/api/v1' })); app.get('/health', (_req, res) => res.json({ success: true, data: { status: 'ok' } })); app.use('/docs', swaggerUi.serve, swaggerUi.setup(swaggerDocument)); app.use('/api/v1/auth', authRoutes); app.use('/api/v1/resume', auth, resumeRoutes); app.use('/api/v1/analysis', auth, analysisRoutes); app.use(errorHandler);
